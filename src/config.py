from dotenv import load_dotenv
import os

# Was trying to host the app on Render, but it didn't work
# and I don't need it now, so commenting out the dotenv loading
# load_dotenv()  # Load variables from .env
# prompt_api_key = os.getenv("API_KEY")

TEMP_DIR = "TEMP"
